# Back-End Development Rules

## Back-End Rules

- Respect and follow the project design pattern: thin/action controller and service -> repository.
- Use `x:laravel-best-practices` skill to implement backend code.
- Use `x:pest-testing` to write tests.
- After finishing backend implementation, check `docs/fe-integrations` and decide whether to create or update docs using `x:fe-integration-docs`.
