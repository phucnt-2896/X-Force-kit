### Backend rules
- Respect and follow the project design pattern (Thin/action controller, service -> repository pattern)
- Use `x:laravel-best-practices` skill to implement backend
- Use `x:pest-testing` to write the test
- After finish you implementation in back-end you MUST check the FE integration docs in `docs/fe-integrations`, then decide to create/update if needed using `x:fe-integration-docs` skill
